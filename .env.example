# Daily-NCAAF local research configuration
# Copy to .env locally if desired. Never commit a populated .env file.

# CollegeFootballData API bearer token used only by credential-gated Phase B probes
CFBD_API_KEY=
